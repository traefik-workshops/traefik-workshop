apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: whoami
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`crd.wrks.demo.traefiklabs.tech`)
      services:
        - kind: Service
          name: whoami-svc
          port: 80
